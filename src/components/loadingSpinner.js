import React from 'react';

const LoadingSpinner = () => (
    <div>
        <i className="fa fa-spinner fa-spin" /> Waiting for new identity to be deployed to the blockchain which requires a transaction confiramtion and block to be mined...
    </div>
);

export default LoadingSpinner;